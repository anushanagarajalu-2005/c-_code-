#include <stdio.h>

int main() {
    int n, i, sum = 0;

    printf("--- Natural Number Sum Calculator ---\n");
    
    // Input the limit
    printf("Enter a positive integer (n): ");
    scanf("%d", &n);

    if (n < 1) {
        printf("Please enter a natural number (greater than 0).\n");
    } 
    else {
        // Loop to calculate the sum
        for (i = 1; i <= n; i++) {
            sum += i; // sum = sum + i
        }

        printf("\nThe sum of the first %d natural numbers is: %d\n", n, sum);
        
        // Verification using the formula
        printf("Verification using formula (n*(n+1)/2): %d\n", (n * (n + 1)) / 2);
    }

    return 0;
}
o/p:
--- Natural Number Sum Calculator ---
Enter a positive integer (n): 10

The sum of the first 10 natural numbers is: 55
Verification using formula (n*(n+1)/2): 55
