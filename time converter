#include <stdio.h>

int main() {
    int total_minutes, hours, minutes;

    printf("--- Time Converter (Minutes to Hours) ---\n");

    // Input the total minutes
    printf("Enter total minutes: ");
    scanf("%d", &total_minutes);

    // Calculate hours using integer division
    hours = total_minutes / 60;

    // Calculate remaining minutes using the modulo operator (%)
    minutes = total_minutes % 60;

    // Display the result
    printf("\n%d minutes is equivalent to: %d Hours and %d Minutes\n", 
            total_minutes, hours, minutes);

    return 0;
}
o/p:
--- Time Converter (Minutes to Hours) ---
Enter total minutes: 142

142 minutes is equivalent to: 2 Hours and 22 Minutes
