#include <stdio.h>

int main() {
    int n, i;
    float marks, total_sum = 0;

    printf("--- Bulk Marks Calculator ---\n");

    // Get the number of students
    printf("Enter the number of students (n): ");
    scanf("%d", &n);

    // Loop to collect marks for 'n' students
    for (i = 1; i <= n; i++) {
        printf("Enter marks for student %d: ", i);
        scanf("%f", &marks);
        
        // Add the current marks to the running total
        total_sum = total_sum + marks;
    }

    // Display the final result
    printf("\n----------------------------");
    printf("\nTotal Marks of %d students: %.2f\n", n, total_sum);
    printf("----------------------------\n");

    return 0;
}
o/p:
--- Bulk Marks Calculator ---
Enter the number of students (n): 4
Enter marks for student 1: 85
Enter marks for student 2: 90
Enter marks for student 3: 95
Enter marks for student 4: 100

----------------------------
Total Marks of 4 students: 370.00
----------------------------
