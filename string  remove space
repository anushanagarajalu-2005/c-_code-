#include <stdio.h>
#include <string.h>

int main() {
    char str[150];
    int i, j = 0;

    printf("Enter a string: ");
    fgets(str, sizeof(str), stdin);

    // Iterating through the string
    for (i = 0; str[i] != '\0'; i++) {
        // If the character is not a space, move it to the 'j' index
        if (str[i] != ' ') {
            str[j] = str[i];
            j++;
        }
    }
    
    // Append the null terminator at the new end of the string
    str[j] = '\0';

    printf("String after removing spaces: %s", str);

    return 0;
}
o/p:
Enter a string: c programming is fun
String after removing spaces: cprogrammingisfun
